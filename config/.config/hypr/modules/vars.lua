local fixi = "$HOME/.dotfiles/Fixi"

return {
    mainMod      = "SUPER",
    fixi         = fixi,
    bar          = fixi .. "/bar/bar.py",
    terminal     = "kitty",
    browser      = "vivaldi",
    browserClass = "vivaldi-stable",
    chat         = "telegram-desktop",
}
